name: Deploying production Branch

on:
  push:
    branches:
      - production

jobs:
  build:
    runs-on: [self-hosted, quecko-mainnsite-vm]

    env:
      PM2_PROCESS_NAME: Quecko-Production-Landing
      PORT: 3010
      node-version: 22

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          clean: false

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.node-version }}

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Create build
        run: npm run build

      - name: Start or Reload the Application
        run: |
          if pm2 list | grep -q "$PM2_PROCESS_NAME"; then
            echo "Process $PM2_PROCESS_NAME exists. Reloading..."
            pm2 reload "$PM2_PROCESS_NAME"
          else
            echo "Process $PM2_PROCESS_NAME does not exist. Starting..."
            PORT=$PORT pm2 start "npm start" --name "$PM2_PROCESS_NAME"
          fi
          pm2 save
